#include <stdlib.h>

void* realloc(void* ptr, size_t new_size)
{
  (void) ptr;
  (void) new_size;
  // TODO: realloc is not supported and will not, just remove realloc
  // from all the other code
  return NULL;
}
